/**
 * Created by KVUSH-NBOOK on 14.11.2015.
 */

/*$('#catalog_menu').tree({
    expanded: '#group'+window.folderN
});*/

/*var folderName = window.location.toString().match(/(?:electro\/|svet\/)([\w+\-\~]+)/);
var section = window.location.toString().match(/\belectro\b|\bsvet\b/);
$.get(
    "http://domain/products/catalog",
    {
        section: section[0],
        grName: folderName[1]
    },
    onAjaxSuccess
);

function onAjaxSuccess(data)
{
    $('#catalog_menu').tree({
        expanded: '#group'+data
    });
}
*/